# TS9Designs Website

This repository contains the active website files at the workspace root.

## Active Editing Scope

- Edit pages in the root (for example: `index.html`, `about.html`, `permit-estimate.html`, `contact.html`).
- Edit active content in `case-studies/` and `assets/`.
